"""Seed script placeholder.

TODO: extract seed logic from ``src/server_orchestrator/main.py`` lifespan
(DB init + first-boot dish seed) into a ``seed()`` function here. The plan
defers this so the phase 5 commit stays a pure file-move refactor; a later
phase can split lifespan into ``data/seed.py`` (seed) + ``data/db.py`` (schema).
"""
